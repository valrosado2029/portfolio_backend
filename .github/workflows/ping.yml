name: Keep backend warm
on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 minutes
  workflow_dispatch:        # also allow manual triggers

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit /health
        run: curl -fsS https://portfolio-backend-miof.onrender.com/health
